# План доработки диплома

## Контекст

Диплом: «Мониторинг и тестирование веб-сервисов»
Объект: RTK AI-агент (FastAPI + LLM + MCP + RAG)
Формат: два слоя — продукт + исследование

---

## Два слоя диплома

**Продукт** — тест-сьют для RTK-агента (128 автотестов A0).
Это реальный артефакт для реального проекта.

**Исследование** — сравнительный эксперимент A0–A3 × O0–O2.
Это знание: какой подход какой класс сбоев обнаруживает.

**Проблема:** тесты написаны до экспериментов, поэтому связь между
слоями не артикулирована. Эксперимент не повлиял на тест-сьют.

**Решение:** чеклисты и тест-кейсы строить явно на основе выводов
эксперимента. Тогда эксперимент становится обоснованием тест-сьюта.

---

## Что эксперимент показал → что добавить в тест-сьют

| Вывод эксперимента | Что добавить |
|---|---|
| BUG-007: latency невидима без O1 | Чеклист: проверка времени ответа |
| BUG-006, 008: error/timeout ловятся O0 | Тест-кейсы: поведение при недоступности |
| BUG-001..005: регрессии при мутации данных | Тест-кейсы: поведение при изменении данных |

---

## Новый экспериментальный вопрос (главная уникальность)

**Проблема:** текущий эксперимент покрывает только инфраструктурные
сбои (latency, timeout, error rate). Это стандарт из литературы
(Gray 1985, Hsueh 1997, Netflix).

**RTK-агент — LLM-система.** У неё есть специфические классы сбоев,
которых нет в классической литературе и которые стандартный
мониторинг (O0/O1/O2) не покрывает вообще.

### LLM-специфичные fault классы (гипотетически)

- LLM недоступен (Ollama/OpenRouter down)
- LLM latency degradation (генерация токенов медленная)
- LLM quality degradation (галлюцинации, нерелевантные ответы)
- MCP Server failure
- RAG возвращает нерелевантный контекст
- Переполнение контекста диалога
- Обрыв контекста между турами

**Ключевой вопрос:** какие из этих сбоев вообще обнаруживаются
стандартными тестами и мониторингом? Гипотеза: quality degradation
не обнаруживается ничем из существующего — сервис UP, метрики в норме,
но ответы неправильные.

Это был бы по-настоящему новый результат.

---

## Конкретный план действий

### Шаг 1 — изучить LLM-интеграцию в коде
Посмотреть как устроены вызовы Ollama/OpenRouter, MCP, RAG.
Понять какие LLM-специфичные сбои реально возможны в этой системе.

### Шаг 2 — составить список LLM-fault классов
Конкретно под RTK-агент, не абстрактно.

### Шаг 3 — новый эксперимент
Проверить что из LLM-fault классов ловится O0/O1/O2,
а что вообще никаким стандартным мониторингом не обнаруживается.

### Шаг 4 — чеклисты и тест-кейсы
Строить с учётом:
- выводов инфраструктурного эксперимента (A0–A3)
- выводов LLM-специфичного эксперимента (шаг 3)

---

## Нарратив для защиты

> Мы тестируем не просто веб-сервис, а LLM-агент.
> Классические подходы (тесты + стандартный мониторинг) покрывают
> инфраструктурные сбои. Но LLM-специфичные сбои — деградация качества,
> контекстные проблемы — остаются невидимыми. Мы это измерили и показали.

---

## Результаты LLM-эксперимента (2026-05-16)

### Матрица обнаружения (6 сценариев × O0/O1/O2)

| Сценарий | O0 | O1 | O2 | Семантика | Вывод |
|---|:---:|:---:|:---:|:---:|---|
| NORMAL | ✗ | ✗ | ✗ | ✓ | базовая линия |
| LLM_UNAVAILABLE | ✓ | ✓ | ✓ | ✗ | обнаруживается — exception |
| LLM_SLOW (2000ms) | ✗ | ✓ | ✓ | ✓ | зеркало BUG-007 |
| **LLM_NO_TOOL_CALLS** | **✗** | **✗** | **✗** | **✗** | **НЕВИДИМЫЙ** |
| LLM_WRONG_TOOL | ✓ | ✓ | ✓ | ✗ | обнаруживается — MCP error |
| **LLM_WRONG_DIAGNOSTIC** | **✗** | **✗** | **✗** | **✗** | **НЕВИДИМЫЙ** |

### Ключевые выводы

**LLM_WRONG_DIAGNOSTIC** — наиболее опасный класс:
- LLM вызывает реально существующий инструмент (list_otrs_comments)
- MCP вызов успешен, HTTP 200, нет ошибок, нормальная latency
- Агент диагностировал не ту проблему
- Невидим для O0/O1/O2 полностью

**LLM_NO_TOOL_CALLS** — агент пропустил всю диагностику:
- LLM вернул только текст, без вызова инструментов
- Никаких сигналов в мониторинге

Оба требуют **семантической валидации** (SemanticChecker) — нового уровня проверки.

### Параллель с инфраструктурным экспериментом
LLM_SLOW повторяет паттерн BUG-007: latency невидима O0, видна O1.
Методологически согласованный результат — оба слоя (инфра + LLM) ведут себя одинаково.

### Код эксперимента
`rtk/experiments/llm_experiment/` — `python -m experiments.llm_experiment`

---

## Текущий статус (на момент записи)

- 128 тестов написаны и проходят
- Эксперименты A0–A3 × O0–O2 завершены
- BUG-001..010 воспроизводимы
- **LLM-специфичный эксперимент завершён (2026-05-16)**
- **Шаг 4 завершён (2026-05-17) — чеклисты и тест-кейсы**

## Итог Шага 4 (2026-05-17)

### Созданные артефакты

| Файл | Назначение |
|---|---|
| `experiments/checklists/checklist.py` | 12 CHK-пунктов: CHK-I-01..06 (инфра) + CHK-L-01..06 (LLM) |
| `tests/test_experiment_derived.py` | 7 тестов: BUG-007, BUG-006/008, BUG-001..005 |
| `tests/test_llm_semantic.py` | 20 тестов: SemanticChecker + invisible faults + contrast |

**Итого тестов: 116 (было 89, добавлено 27). Все проходят.**

### Структура чеклиста

```
CHK-I-01  O1  SLA test: get_order_status < 50ms (← BUG-007)
CHK-I-02  O1  O0 не видит latency, O1 видит (← BUG-007)
CHK-I-03  O0  ConnectionError фиксируется в action log (← BUG-006)
CHK-I-04  O0  TimeoutError фиксируется в action log (← BUG-008)
CHK-I-05  O0  Изменение статуса заказа отражается (← BUG-001..005)
CHK-I-06  O0  Ex-vivo replay детектирует мутацию (← BUG-001)

CHK-L-01  semantic  SemanticChecker: пустые actions → FAIL
CHK-L-02  semantic  NoToolCallsProvider невидим O0/O1/O2 (← LLM_NO_TOOL_CALLS)
CHK-L-03  semantic  WrongDiagnosticProvider невидим O0+O1+O2 (← LLM_WRONG_DIAGNOSTIC)
CHK-L-04  semantic  Wrong tool имеет ok=True — механизм невидимости
CHK-L-05  semantic  NormalProvider проходит семантику
CHK-L-06  O1  LLM_SLOW: O0 не видит, O1 видит — зеркало BUG-007
```

### Ключевой тест для диплома: `test_wrong_diagnostic_invisible_to_all_levels`
Энкодирует главный вывод: LLM_WRONG_DIAGNOSTIC невидим для O0, O1, и O2 одновременно.
Запускается при `pytest -m experiment`.
